# Azure Key Vault Module

Creates an Azure Key Vault using Azure RBAC authorization.

## Security settings

- Azure RBAC authorization enabled
- Soft-delete retention configurable
- Purge protection enabled by default
- Public network access configurable

Private endpoints and private DNS are managed separately.