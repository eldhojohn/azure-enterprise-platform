# Azure Private DNS Zone Module

Creates an Azure Private DNS zone and optionally links it to one or more
virtual networks.

DNS record registration should normally remain disabled for Azure Private
Endpoint zones.