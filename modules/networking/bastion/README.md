# Azure Bastion Module

Creates:

- Azure Bastion host
- Standard static public IP
- Bastion IP configuration using an existing AzureBastionSubnet

## Requirements

The supplied subnet must be named `AzureBastionSubnet`.